# TASK-103: Frontend Auth (3d)
Halaman login (customer + owner terpisah), register, forgot password. Auth guard, token persist.

Referensi: DESIGN.md → Customer Screens (baris 240-433), Owner Login (baris 440-452); API Spec → /auth/*
Output: routing protected, redirect sesuai role, error form dari API.
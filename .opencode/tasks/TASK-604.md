# TASK-604: Production Setup (2d)
Production Dockerfiles (Laravel php-fpm + nginx, React nginx static). SSL cert. Docker Compose production override. Deploy script.

Referensi: ARCHITECTURE.md → Deployment (baris 482-521); WORKFLOW.md → Deploy
Output: production server running, HTTPS, build otomatis via CI.
# TASK-505: Customer Profile (1d)
`GET /profile`, `PUT /profile`. Edit name, phone, avatar. Logout button.

Referensi: DESIGN.md → Profile (baris 422-433); API Spec → baris 258-277 (GET /auth/me)
Customer only. Upload avatar.
KERJAKAN: # TASK-301: Service CRUD API (2d)
`GET /services`, `POST /services`, `PUT /services/{id}`, `DELETE /services/{id}`.

Referensi: API Spec.md → Service API (baris 453-517); SCHEMA.md → services table; RULES.md → BR-010, BR-011
Owner only. Duration (menit), price (decimal). Soft delete.
# TASK-106: Role Permission System (2d)
Middleware `role:super_admin,owner,customer`. Gates/Policies per aksi.

Referensi: RULES.md → BR-003, BR-022, BR-023; ARCHITECTURE.md → Authorization (baris 242-265); API Spec → Permission Matrix
Roles: super_admin (all tenants), owner (own tenant), customer (own data).